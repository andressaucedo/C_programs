/*****************************************************************************
 * myfilecopy.c - This program takes two arguments from the command line, the*
 * first is an existing file to be copied and the second is a file copy to be*
 * written out in binary mode or standard i/o.                               *
 *****************************************************************************/
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>
#include<unistd.h>


int main(int argc, char const *argv[]) {
  /*BEGIN error checking block for command line call.
  if(argc != 3 && argc != 4){
    fprintf(stderr,
        "usage: myfilecopy sourcefile copyfile [-s/--standard] [-b/--binary]\n"
        "if no options are included the copy is written in binary mode.");
    exit 1;
  }
  if(alnum[argv
*/

  return 0;
}
